# FastAPI JWT Auth CRUD (Pet Project) 🚀

Простой и мощный сервис авторизации на FastAPI с использованием JWT-токенов, ролевой моделью доступа и полным покрытием тестами.

## 🛠 Функционал
- **Регистрация и Логин**: Хеширование паролей (pwdlib) и выдача JWT.
- **Профиль пользователя**: Возможность просмотра и частичного обновления своих данных (PATCH).
- **Админ-панель**: 
    - Управление ролями (назначение админов).
    - Система блокировки пользователей (Ban/Unban).
    - Мягкое удаление аккаунтов (Soft Delete).
- **Безопасность**: Проверка активности аккаунта на каждом запросе.
- **Тестирование**: 100% покрытие бизнес-логики (pytest + httpx).

## 🚀 Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com
   cd ТВОЙ_РЕПОЗИТОРИЙ

2. **Создайте виртуальное окружение и установите зависимости:**
   ```bash
   python -m venv venv
   source venv/Scripts/activate  # Для Windows
   pip install -r requirements.txt

3. **Настройте переменные окружения:**
  Создайте файл .env в корне:
   ```bash
   SECRET_KEY=your_super_secret_key
   ALGORITHM=HS256
   ACCESS_TOKEN_EXPIRE_MINUTES=30
    ```
4. **Запустите сервер:**
Создайте файл .env в корне:
   ```bash
   python main.py
   ```
Документация будет доступна по адресу: 127.0.0.1:8000 

## 🧪 Тестирование

Для запуска тестов используйте:

```bash
pytest -v
```

## 🏗 Стек технологий:
- FastAPI
- Pytest / HTTPX
- Pydantic V2
- PyJWT
- Pwdlib
